import { StyleSheet } from 'react-native';

export const GlobalStyles = StyleSheet.create({
  content: {
    padding: 8,
    justifyContent: 'center',
    flex: 1,
    // maxWidth: 1200,
    // borderWidth: 2,
    alignItems: 'center',
    margin: 8,
  },
  container: {
    padding: 16,
    justifyContent: 'center',
  },
});
