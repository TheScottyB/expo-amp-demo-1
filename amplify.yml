version: 1.0
frontend:
  phases:
    preBuild:
      commands:
        - yarn
        - nvm use $VERSION_NODE_12
        - npm install --quiet --global expo-cli    
    build:
      commands:
        - expo build:web
  artifacts:
    baseDirectory: web-build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - $(npm root --global)/**/*
